apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "borg-server.fullname" . }}-test-connection"
  labels:
    {{- include "borg-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "borg-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
