apiVersion: v1
kind: Service
metadata:
  name: {{ include "borg-fleet.fullname" . }}
  labels:
    {{- include "borg-fleet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "borg-fleet.selectorLabels" . | nindent 4 }}
