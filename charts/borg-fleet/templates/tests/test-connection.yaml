apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "borg-fleet.fullname" . }}-test-connection"
  labels:
    {{- include "borg-fleet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "borg-fleet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
